#include <catch2/catch_test_macros.hpp>
#include <catch2/generators/catch_generators.hpp>
//#include <vector>

#include "geowars/components.hpp"
using namespace GWars;

TEST_CASE( "Initialize component", "[component]" ) {
	/*SECTION( "Initalize with Vec2 default parameters" ) {
		Vec2 a;
		CTransform Ct(a, a, 0);
		CHECK( Ct.pos == a );
		CHECK( Ct.velocity == a );
		CHECK( Ct.angle == 0.0 );
	}
	
	SECTION( "Initialize with vec2 defined parameters" ) {
		Vec2 pos(2.0, 6.0);
		Vec2 velocity(4.0, 15.0);
		CTransform a_Ct(pos, velocity, 10);

		CHECK( a_Ct.pos == pos );
		CHECK( a_Ct.velocity == velocity );
		CHECK( a_Ct.angle == 10 );
	}
	*/
}
